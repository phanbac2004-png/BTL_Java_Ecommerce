# SMTP configuration for Kiddy Shop password reset emails
# -------------------------------------------------------
# Copy this file to src/conf/smtp.properties (already here) and fill in your SMTP credentials.
# When you build/deploy, it will be placed in WEB-INF/classes so EmailUtil can load it.

# Gmail example:
# smtp.user=yourgmail@gmail.com
# smtp.pass=your-app-password
# smtp.host=smtp.gmail.com
# smtp.port=587
# smtp.from_name=Kiddy Shop Support

# === REQUIRED ===
smtp.user=phanbac2004@gmail.com
smtp.pass=123

# === OPTIONAL (defaults shown) ===
smtp.host=phanbac2004@gmail.com
smtp.port=587
smtp.from_name=Kiddy Shop Support

